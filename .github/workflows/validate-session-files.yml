name: Validate session files

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  check-session-files:
    name: Validate session JSON files
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate session files
        run: scripts/validate-sessions.sh
